AFurry - Uses WAMP stack environment
	A site where I tested account management techniques, as well as basic site
	layout funcitons

FurMUDer - Uses MEAN stack environment
	A site using websockets to create a server where users could post a message,
	and all connected users would have their pages automatically updated as soon
	as the message is recieved.

